# macOS
.DS_Store
._*

# Backups criados pelo stow ao resolver conflitos
*.antares-bak

# Locks/artefatos
Brewfile.lock.json

# Estado de runtime do Neovim (gerado localmente por máquina)
stow/nvim/.config/nvim/lazy-lock.json
stow/nvim/.config/nvim/lazyvim.json

# Codex — snippets gerados (build a partir dos .md)
library/codex/.build/

# Nada de identidade/segredos aqui (eles vivem fora do repo, em ~/.gitconfig.local)

# Python bytecode
__pycache__/
*.pyc
