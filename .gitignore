# macOS
.DS_Store
._*

# Backups criados pelo stow ao resolver conflitos
*.antares-bak

# Locks/artefatos
Brewfile.lock.json

# Nada de identidade/segredos aqui (eles vivem fora do repo, em ~/.gitconfig.local)
