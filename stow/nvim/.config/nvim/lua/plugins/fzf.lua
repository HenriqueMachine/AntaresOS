-- AntaresOS · Neovim — habilita o fzf-lua (extra do LazyVim) como picker
-- Rastreável no repo (ao contrário do lazyvim.json, que o LazyVim reescreve).
return {
  { import = "lazyvim.plugins.extras.editor.fzf" },
}
